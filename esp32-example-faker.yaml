<<: !include esp32-example-debug.yaml

jbd_bms:
  - id: bms0
    uart_id: uart_0
    update_interval: 2s
    rx_timeout: ${rx_timeout}
    enable_fake_traffic: true
    modbus_id: 0
  - id: bms1
    uart_id: uart_0
    update_interval: 2s
    rx_timeout: ${rx_timeout}
    enable_fake_traffic: true
    modbus_id: 1
  - id: bms2
    uart_id: uart_0
    update_interval: 2s
    rx_timeout: ${rx_timeout}
    enable_fake_traffic: true
    modbus_id: 2